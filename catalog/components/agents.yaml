schema_version: 1
components:
  - id: claude-code
    name: Claude Code
    kind: agent
    provides: [claude-code]
    dependencies: [bun]
    version_policy:
      mode: pinned
      lock_key: claude-code
    verification:
      command: [claude, --version]
    targets:
      macos-host:
        status: supported
        installer: bun
        package: "@anthropic-ai/claude-code"
      windows-host:
        status: supported
        installer: bun
        package: "@anthropic-ai/claude-code"
      wsl-guest:
        status: unsupported
        reason: AI coding agents belong to the macOS or Windows host workspace.
      lima-guest:
        status: unsupported
        reason: AI coding agents belong to the macOS or Windows host workspace.

  - id: opencode
    name: OpenCode
    kind: agent
    provides: [opencode]
    dependencies: [bun]
    version_policy:
      mode: pinned
      lock_key: opencode
    verification:
      command: [opencode, --version]
    targets:
      macos-host:
        status: supported
        installer: bun
        package: opencode-ai
      windows-host:
        status: supported
        installer: bun
        package: opencode-ai
      wsl-guest:
        status: unsupported
        reason: AI coding agents belong to the macOS or Windows host workspace.
      lima-guest:
        status: unsupported
        reason: AI coding agents belong to the macOS or Windows host workspace.

  - id: codex
    name: OpenAI Codex
    kind: agent
    provides: [codex]
    dependencies: [bun]
    version_policy:
      mode: pinned
      lock_key: codex
    verification:
      command: [codex, --version]
    targets:
      macos-host:
        status: supported
        installer: bun
        package: "@openai/codex"
      windows-host:
        status: supported
        installer: bun
        package: "@openai/codex"
      wsl-guest:
        status: unsupported
        reason: AI coding agents belong to the macOS or Windows host workspace.
      lima-guest:
        status: unsupported
        reason: AI coding agents belong to the macOS or Windows host workspace.
